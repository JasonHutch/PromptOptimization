# Task
Classify the provided domain phrases into the parts of a software system (classes, attributes, associations, and similar).

## Domain Phrases

<DOMAIN PHRASES>

## Labels
Use these labels for the classification:

- C = class
- A = attribute
- V = attribute value
- AS = association
- AC = association class
- AG = aggregation
- I = inheritance

# Output
Output raw CSV text with exactly this header row, followed by one row per phrase.

label,element,arg1,arg2

Output nothing except the CSV text.